/**
 * Colored stage chip for the Pages list view, now shared via
 * @rinsly-com/site-core. Re-exported here because Payload resolves admin
 * component paths against this app — see WorkflowAction.tsx.
 */
export {
  WorkflowStatusCell,
  WorkflowStatusCellDefault as default,
} from '@rinsly-com/site-core/admin'
