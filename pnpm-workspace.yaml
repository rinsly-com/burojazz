# Security floors for vulnerable TRANSITIVE packages (GitHub dependabot
# alerts, 2026-08-15). Each pins a range that has a patched release; direct
# parents (next, payload, wrangler) still pull older ones. Re-check and drop
# entries whenever the parents bump: pnpm why <pkg>.
#
# Deliberately ABSENT: undici (5 alerts, fixed in 7.29.0). Forcing 7.29.0
# breaks miniflare's fetch under the Next dev server (every next/font
# download fails and the frontend 500s), and undici only runs in local dev
# tooling here (miniflare/wrangler/jsdom), never in the deployed Worker.
# Accepted until wrangler/next move to it themselves.
overrides:
  'postcss@<8.5.23': 8.5.23
  'js-yaml@>=4.0.0 <4.3.1': 4.3.1
  'fast-uri@>=3.0.0 <3.1.5': 3.1.5
  'brace-expansion@>=2.0.0 <2.1.3': 2.1.3
  'brace-expansion@>=4.0.0 <5.0.9': 5.0.9
  'nanoid@<3.3.16': 3.3.16
  'dompurify@<3.4.13': 3.4.13
allowBuilds:
  core-js-pure: true
  esbuild: true
  sharp: true
  unrs-resolver: true
  workerd: true
minimumReleaseAgeExclude:
  - '@rinsly-com/site-core'
patchedDependencies:
  '@payloadcms/drizzle@3.85.2': patches/@payloadcms__drizzle@3.85.2.patch
